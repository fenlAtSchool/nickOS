try{
let j = eval(kernel.getFile(data).contents)
log("pack.cff", `Succesfully decompressed file ${data}`)
}
catch(error){
log("pack.cff", `Unable to decompress file ${data}: Error ${error}`)
}
"HALT"
